language: python
python:
  - "3.2"
  - "3.3"
# command to install redisco with dependencies
install:
  - pip install . --use-mirrors
  - pip install testfixtures==3.0.1
# command to run tests
script: nosetests --with-doctest
services:
  - redis-server
